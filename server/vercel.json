{
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs22.x"
    }
  },
  "outputDirectory": ".",
  "buildCommand": "echo 'skip build'",
  "installCommand": "corepack enable && yarn install --immutable"
}
