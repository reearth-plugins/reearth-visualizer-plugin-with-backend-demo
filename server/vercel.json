{
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs22.x"
    }
  },
  "outputDirectory": ".",
  "buildCommand": "yarn build",
  "installCommand": "corepack enable && yarn install --immutable"
}
